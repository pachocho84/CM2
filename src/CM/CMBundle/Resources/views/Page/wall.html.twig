{% extends 'CMBundle:Page:layout.html.twig' %}

{% block title %}{{ '%page%&#39;s wall'|trans({'%page%': page})|raw }}{% endblock %}

{% block body %}
    {{ include('CMBundle:Page:top.html.twig', {'inPage': 'wall', 'page': page}) }}
    
    {#<ul class="breadcrumb">
      <li><?php echo link_to('Home', '@homepage') ?></li>
      <li class="active">{{ block('title') }}</li>
    </ul>#}


    <div class="box">
        <div class="box-top">
            <h3>{{ show_icon('Wall')|raw }} <span>{{ 'Last posts'|trans }}</span></h3>
        </div>
        <ul class="media-list objects">
            {{ include('CMBundle:Wall:posts.html.twig', {'slug': page.slug}) }}
        </ul>
    </div>

{% endblock %}