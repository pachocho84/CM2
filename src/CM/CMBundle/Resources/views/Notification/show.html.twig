{% extends 'CMBundle::layout.html.twig' %}

{% block heading %}
    {#{{ include('CMBundle:User:top.html.twig') }}#}
{% endblock %}

{% block body %}
    <h2>
        {{ icon('Notification') }} {{ 'Notifications'|trans }}
    </h2>                                                                    

    <div class="objects-container">
        <div class="objects">
            <ul class="media-list">
                {{ include('CMBundle:Notification:objects.html.twig', {box: true}) }}
            </ul>
        </div>
    </div>
{% endblock %}