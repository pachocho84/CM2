{% extends 'CMBundle:Form:layout.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-lg-5">
            <h2>{{ block('title') }}</h2>
        </div>
        {{ include('CMBundle:Multimedia:menu.html.twig') }}
    </div>
    
    {{ form(form) }}
{% endblock %}