{% extends 'CMBundle::layout.html.twig' %}

{% block title %}{{ 'Multimedia'|trans }}{% endblock %}

{% block body %}

    <div class="row">
        <div class="col-lg-5">
            <h2>{{ block('title') }}</h2>
        </div>
        {{ include('CMBundle:Multimedia:menu.html.twig') }}
    </div>

    {{ include('CMBundle:Multimedia:objects.html.twig') }}

{% endblock %}