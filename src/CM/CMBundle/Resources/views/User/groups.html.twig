{% extends 'CMBundle:User:accountLayout.html.twig' %}

{% block title %}{{ 'Groups'|trans }}{% endblock %}

{% block body %}

    <div class="box">
        <div class="box-heading">
            <h2>{{ icon('Group') }} <span>{{ block('title') }}</span></h2>
        </div>

        <div class="box-body">
            {{ include('CMBundle:User:groupObjects.html.twig') }}
        </div>
    </div>
  
{% endblock %}