{% extends 'CMBundle::layout.html.twig' %}

{% block title %}{{ '%user%&#39;s wall' | trans({'%user%': user}) | raw }}{% endblock %}

{% block body %}
    {{ include('CMBundle:User:top.html.twig', {'inPage': 'wall'}) }}
    
    {#<ul class="breadcrumb">
      <li><?php echo link_to('Home', '@homepage') ?></li>
      <li class="active">{{ block('title') }}</li>
    </ul>#}


    <div class="box">
        <div class="box-top">
            <h3>{{ show_icon('Wall') | raw }} <span>{{ 'Last posts' | trans }}</span></h3>
        </div>
        <ul class="media-list objects">
            {{ include('CMBundle:Wall:posts.html.twig', {'slug': user.slug}) }}
        </ul>
    </div>

{% endblock %}