{% extends 'CMBundle::layout.html.twig' %}

{% block body %}
    {{ user }} profile
    
    {{ render(controller('CMBundle:Fan:fanButton', {'userId': user.id, 'fanBecomeText': '<i class="glyphicon glyphicon-flag"></i> '~('Become fan of %user%'|trans({'%user%': user.firstName}))})) }}
{% endblock %}
