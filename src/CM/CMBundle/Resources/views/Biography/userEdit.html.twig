{% extends 'CMBundle:User:accountLayout.html.twig' %}

{% block title %}{{ 'Biography'|trans }}{% endblock %}

{% block body %}

    <div class="box">
        <div class="box-heading">
            <h2>{{ icon('Biography')|raw }} {{ block('title') }}</h2>
        </div>
        <div class="box-body">
            {{ form(form) }}
        </div>
    </div>
    
{% endblock %}