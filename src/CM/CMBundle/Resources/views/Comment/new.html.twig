<li class="media comment comment_new {# <?php $comments->count() == 0 && $sf_request->getParameter('action') != 'show' and print ' hide' ?> #}">
    {{ show_img_box(app.user.img | imagine_filter('350'), {'width': 32, 'height': 32, 'offset': app.user.offset,  'link': path('user_show', {'slug': app.user.slug}), 'default': 'user', 'box_attributes': {'class': 'pull-left'}}) | raw }}
    <div class="media-body">      
            {{ form_start(form) }}
                {{ form_widget(form.comment, {'container_class': 'col-lg-12'}) }}
                <input type="submit" name="commit" value="{{ 'Send' | trans }}" class="pull-right btn btn-mini hide" />
            {{ form_end(form) }}
    </div>
</li>