<li class="media comment{{ (hide is defined and hide) ? ' hide' : '' }}">

    {% if false %} {# TODO: user->canManage($comment) #}
       {# {{ delete_link(path('comment_delete', {'id': comment.id}), 'comment', {'data-placement': 'left', 'class': 'close', 'text': '&times;', 'icon': false}) }} #}
    {% endif %}

    {{ show_img_box(comment.user.img, {'width': 32, 'height': 32, 'offset': comment.user.offset, 'default': 'user', 'box_attributes': {'class': 'pull-left'}}) | raw }}
{# <?php show_img_box($comment->getUser()->getImg(), array('width' => 32, 'height' => 32, 'offset' => $comment->getUser()->getImgOffset(), 'default' => 'user', 'box_attributes' => array('class' => 'pull-left'))) ?> #}

    <div class="media-body">
        <a href="{{ path('user_show', {'username': comment.user.usernameCanonical}) }}" target="_blank">{{ comment.user }}</a> {{ comment.comment }}
        <div class="text-muted">
            {{ '%date% at %time%' | trans({'%date%': comment.createdAt | format_date('d MMMM Y'), '%time%': comment.createdAt | format_date('HH:mm')}) }}
        </div>
    </div>

    
</li>