{% if post.comments.count > 0 %}
    <a href="{# {{ path('post_show', {'post_id': postId}) }} #}" class="bottom-comment-count" ><i class="glyphicon glyphicon-comment"></i> {{ post.comments.count }}</a>
{% else %}
    <span class="bottom-comment-count"></span>
{% endif %}