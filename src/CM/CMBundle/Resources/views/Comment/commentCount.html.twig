{% if post.comments.count > 0 %}
    <a href="{{ path('comment_show', {'id': post.id}) }}" class="bottom-comment-count" ><i class="glyphicon glyphicon-comment"></i> {{ post.comments.count }}</a>
{% else %}
    <span class="bottom-comment-count"></span>
{% endif %}