{% extends 'CMBundle::layout.html.twig' %}



{% block title %}{{ "Events"|trans }}{% endblock %}



{% block body %}

	<div class="row">
		<div class="col-lg-5">
			<h2>{{ block('title') }}</h2>
		</div>
		{{ include('CMBundle:Event:menu.html.twig') }}
	</div>
	
	
	
	<div class='box'>
		{{ include('CMBundle:Event:objects.html.twig', { 'events': events } ) }}
	</div>
	
	<div class="navigation">
    {{ knp_pagination_render(events) }}
	</div>
	
{% endblock %}