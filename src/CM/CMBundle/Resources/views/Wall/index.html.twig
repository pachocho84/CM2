{% extends 'CMBundle::layout.html.twig' %}

{% block title %}{{ 'wall' | trans | capitalize }}{% endblock %}

{% block body %}
    <h2>{{ block('title') }}</h2>
    
    {#<ul class="breadcrumb">
      <li><?php echo link_to('Home', '@homepage') ?></li>
      <li class="active">{{ block('title') }}</li>
    </ul>#}

    <div class="row">
        <div class="col-md-9 col-md-offset-3">
            <div class="box">
                <div class="box-heading">
                    <h3>{{ icon('Wall') | raw }} <span>{{ 'Last posts' | trans }}</span></h3>
                </div>
                <ul class="media-list objects">
                    {{ include('CMBundle:Wall:posts.html.twig') }}
                </ul>
            </div>
        </div>
    </div>

{% endblock %}