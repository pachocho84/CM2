{% extends 'CMBundle::layout.html.twig' %}

{% block title %}{{ 'Post' | trans }}{% endblock %}

{% block body %}
    <h2>{{ block('title') }}</h2>

            <div class="box">
                <div class="box-heading">
                    <h3>{{ show_icon('wall') | raw }} <span>{{ 'Posts' | trans }}</span></h3>
                </div>
                <ul class="media-list objects">
                    {{ include('CMBundle:Wall:post.html.twig')}}
                </ul>
            </div>
{% endblock %}