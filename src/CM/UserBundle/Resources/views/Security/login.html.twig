{% extends 'CMBundle::layout.html.twig' %}



{% block title %}
    {{ 'Login'|trans }}
{% endblock %}



{% block heading %}
    <h2>{{ icon('Login') }} {{ block('title') }}</h2>
{% endblock %}



{% block body %}
    <div class="row">
        <div class="col-md-8">
            <div class='box'>
                <div class="box-body">
                    {{ include('CMUserBundle:Security:form.html.twig') }}
                    {{ render(controller('FOSUserBundle:Registration:register', {'template': null})) }}
                </div>
            </div>
        </div>
        <div class="col-md-4">
        </div>
    </div>
{% endblock %}